#!/bin/bash

# leenium:summary=Apply the current theme keyboard color to ASUS ROG keyboards

ASUSCTL_THEME=~/.config/leenium/current/theme/keyboard.rgb

if leenium-cmd-present asusctl; then
  asusctl aura effect static -c $(sed 's/^#//' $ASUSCTL_THEME)
fi
