#!/bin/bash

CURRENT_THEME_NAME=$(cat "$HOME/.config/leenium/current/theme.name")
THEME_USER_BACKGROUNDS="$HOME/.config/leenium/backgrounds/$CURRENT_THEME_NAME"

mkdir -p "$THEME_USER_BACKGROUNDS"
nautilus "$THEME_USER_BACKGROUNDS"
