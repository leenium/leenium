#!/bin/bash

# leenium:summary=Install the ONCE service, enable its background service, and launch the TUI.
# leenium:requires-sudo=true

echo "Installing ONCE..."
leenium-pkg-aur-add once-bin

echo "Enabling ONCE background service..."
sudo systemctl enable --now once-background.service

echo -e "\nLaunching ONCE..."
once