#!/bin/bash

# leenium:summary=Finish the installation of Leemium with items that can only be done after logging in.
# leenium:requires-sudo=true

set -e

FIRST_RUN_MODE=~/.local/state/leenium/first-run.mode

if [[ -f $FIRST_RUN_MODE ]]; then
  rm -f "$FIRST_RUN_MODE"

  bash "$LEENIUM_PATH/install/first-run/battery-monitor.sh"
  bash "$LEENIUM_PATH/install/first-run/recover-internal-monitor.sh"
  bash "$LEENIUM_PATH/install/first-run/cleanup-reboot-sudoers.sh"
  bash "$LEENIUM_PATH/install/first-run/firewall.sh"
  bash "$LEENIUM_PATH/install/first-run/dns-resolver.sh"
  bash "$LEENIUM_PATH/install/first-run/gnome-theme.sh"
  bash "$LEENIUM_PATH/install/first-run/elephant.sh"
  sudo rm -f /etc/sudoers.d/first-run

  bash "$LEENIUM_PATH/install/first-run/welcome.sh"
  bash "$LEENIUM_PATH/install/first-run/wifi.sh"
fi
