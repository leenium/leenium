#!/bin/bash

# leenium:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/leenium/toggles/hypr/internal-monitor-disable.conf"

if [[ -f $TOGGLE ]] && ! leenium-hw-external-monitors; then
  rm -f "$TOGGLE"
fi