#!/bin/bash

# leenium:summary=Launch a TUI command in the default terminal with Leemium styling
# leenium:args=<command> [args...]

exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.leenium.$(basename $1) -e "$1" "${@:2}"
