#!/bin/bash

# Overwrite the user configs for the Waybar menu bar with the Leenium defaults and restart the service.

leenium-refresh-config waybar/config.jsonc
leenium-refresh-config waybar/style.css
leenium-restart-waybar
