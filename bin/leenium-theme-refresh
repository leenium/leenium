#!/bin/bash

# Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.config/leenium/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  leenium-theme-set "$(cat $THEME_NAME_PATH)"
fi
